# Attribution

## [`xsf`](./xsf-license)

The `special` crate is mostly a port of the `xsf` library to Rust.


## [`statrs`](./statrs-license)

The core API of `distributions` is inspired by the `statrs` version
forked into Aspartik.  And many implementations are direct copies of the
ones in `statrs`.
